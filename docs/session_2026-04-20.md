# 작업 세션 정리 — 2026-04-20

## 오늘 완료한 작업

### 1. Phase 6 탈진 시스템 완성 (6-9)

이전 세션에서 `ClimbingHandler.updateExhaustion()` 메서드 본문이 없어서 컴파일 오류가 발생했던 문제를 수정.

**수정된 파일:**
- `src/main/java/choco/ratel/smartmoving/physics/ClimbingHandler.java`
- `src/main/java/choco/ratel/smartmoving/physics/CeilingClimbingHandler.java`

두 파일 모두 동일한 패턴으로 `updateExhaustion()` 구현:
- `ConfigManager`에서 활성화 여부, 임계값, 게인 값을 읽음
- 클라이밍 중 `exhaustion += gain`, 지면에 있을 때 `exhaustion -= 0.002F` 회복
- 탈진 초과 시 상태 플래그를 `false`로 강제 전환

---

### 2. 애니메이션 전면 수정 — 계층 모델 좌표계 변환 버그

**수정된 파일:** `src/client/java/choco/ratel/smartmoving/mixin/client/PlayerEntityModelMixin.java`

#### 핵심 원인 분석

원본 SmartMoving(1.7.10)과 1.21.1의 모델 구조 차이:

| 구분 | 1.7.10 원본 | 1.21.1 현재 |
|------|------------|------------|
| 모델 구조 | **계층적** — `bipedOuter → bipedTorso → 팔/다리` | **flat** — 모든 파트가 독립적 |
| 각도 의미 | 팔/다리 각도 = **LOCAL** (부모 상대) | 팔/다리 각도 = **WORLD** (절대) |
| 변환 필요 | - | `world = parentPitch(outerX) + local` |

원본에서 `bipedOuter.rotateAngleX = outerX`로 설정했을 때, 팔/다리의 **world 각도** = `outerX + 팔/다리 LOCAL 각도`. 1.21.1 flat 모델에서 `outerX`에 해당하는 오프셋을 각 파트에 직접 더해야 한다.

#### 1차 수정 — LOCAL→WORLD 변환 (이전 세션)

각 애니메이션에서 `bodyPitch(outerX)`를 팔/다리 pitch에 더해 world 공간으로 변환:

```
applyCrawlingAngles   : bodyPitch = QUARTER - THIRTYTWOTH
applySlidingAngles    : bodyPitch = QUARTER
applySwimmingAngles   : outerX = QUARTER - SIXTEENTH*combined
applyDivingAngles     : outerX = QUARTER - vAngle
applyHeadJumpingAngles: outerX = QUARTER - vAngle
```

또한 수영 팔 pitch의 `SIXTEENTH*combined` 상쇄 항을 발견:
```
world arm pitch = outerX + local
= (QUARTER - SIXTEENTH*combined) + cycling + SIXTEENTH*combined
= QUARTER + cycling  ← 단순화됨
```

#### 2차 수정 — head.pitch, arm.yaw 추가 버그 수정 (오늘)

1차 수정 후에도 남아 있던 좌표계 오류 3가지:

**버그 1: 기어가기/슬라이딩 head.pitch**
```java
// 잘못된 코드 (LOCAL 값을 그대로 사용)
m.head.pitch = -bodyPitch;  // flat 모델에서 머리가 뒤로 78~90° 꺾임

// 수정된 코드
m.head.pitch = 0F;  // flat 모델에서 world pitch=0 = 앞 방향
```
원본에서 `head`는 `bipedTorso`의 자식이었으므로 `head.localPitch = -bodyPitch`이면
`head.worldPitch = bodyPitch + (-bodyPitch) = 0`. flat 모델에서는 그냥 `0F`.

**버그 2: 천장 클라이밍 arm.yaw**
```java
// 잘못된 코드 (LOCAL 값을 그대로 사용)
m.leftArm.yaw  = -rotateY;  // 팔이 엉뚱한 방향으로 회전
m.rightArm.yaw = -rotateY;

// 수정된 코드
m.leftArm.yaw  = 0F;  // flat 모델에서 팔은 body의 자식이 아님
m.rightArm.yaw = 0F;
```
원본에서 `-rotateY`는 `bipedOuter.rotateAngleY = rotateY` 부모 yaw를 상쇄하기 위한 LOCAL 값.
flat 모델에서 팔은 body의 자식이 아니므로 상쇄할 부모 yaw가 없다. `0F`이면 world yaw = 0.

**버그 3: 헤드점프 head.pitch**
```java
// 잘못된 코드
m.head.pitch = -outerX / 2F;  // 원본 LOCAL 값 그대로

// 수정된 코드
m.head.pitch = outerX / 2F;   // world pitch = outerX + (-outerX/2) = outerX/2
```
원본 world head pitch = `bipedOuter.pitch + head.localPitch = outerX + (-outerX/2) = outerX/2`.
flat 모델에서는 `outerX/2`를 직접 대입.

---

## 현재 상태

### 빌드 상태
- `./gradlew build` **성공** (커밋: `827e0ab`)
- 게임 내 테스트는 아직 미실시

### 체크리스트 진행 현황
- Phase 1~6: 완료
- Phase 7 (마무리 검증): 미완료 — 게임 내 테스트 필요

---

## 내일 해야 할 작업

### 최우선: 게임 내 애니메이션 테스트

각 상태별로 실제 게임에서 포즈가 의도대로 나오는지 확인:

| 상태 | 확인 포인트 |
|------|-----------|
| 기어가기 | 몸통이 앞으로 숙여지고, 팔/다리가 바닥을 짚는 포즈 |
| 클라이밍 | 팔이 위로 뻗고 교차 동작 |
| 천장 클라이밍 | 팔이 위(천장)쪽으로 뻗고, 몸통이 좌우로 흔들림 |
| 슬라이딩 | 몸통이 앞으로 눕고 팔이 옆으로 벌어짐 |
| 수영 | 몸통이 앞으로 기울고 팔이 번갈아 앞뒤 |
| 잠수 | 바라보는 방향으로 몸 전체가 기울어짐 |
| 헤드점프 | 몸통과 머리가 점프 방향으로 기울어짐 |

### 이상 발견 시 확인 순서

1. `PlayerEntityModelMixin.java` 해당 메서드 각도 값 재확인
2. `docs/research_animation.md` 섹션 D (원본 각도 값) 참조
3. 원본 좌표계 변환 공식: `world = outerX + local`

### Phase 7 체크리스트 항목

`docs/checklist_implementation.md` Phase 7 참조:
- 기능별 단독 테스트 (기어가기, 클라이밍, 천장 클라이밍, 슬라이딩, 수영/잠수, 점프, 탈진)
- 멀티플레이어 테스트 (원격 플레이어 애니메이션 동기화)
- 엣지 케이스 테스트

---

## 세션 2 (이어진 세션) — 애니메이션 추가 수정

### 문제 상황 (유저 보고)

기어가기/잠수 등 모든 SmartMoving 애니메이션에서 파트 분리 문제 지속:
- **기어가기**: 팔이 몸 앞에 이상하게 교차, 다리가 땅속으로 들어가 1자로 서 있음, 몸통과 연결 안됨
- **잠수**: 팔이 이상하게 들려있고 다리가 몸통과 분리됨

### 근본 원인 분석

**문제: setAngles에서 body.pitch만 변경해서는 파트가 연결될 수 없다.**

1.21.1 flat 모델에서 `body.pitch = 79°`를 설정해도:
- body 메쉬만 기울어짐
- arm.pivot, leg.pivot 위치는 변하지 않음 (여전히 world Z=0)
- 원본 1.7.10에서 `bipedTorso` 회전 시 자식(arm/leg)의 3D 위치가 함께 움직이는 효과 재현 불가능

구체적으로: 기어가기(bodyPitch=79°) 시
- 어깨(body 상단) world 위치: Y≈3, Z=0 (pivot 고정)
- 올바른 arm pivot 위치: Y≈3.38, **Z≈1.96** (bipedTorso 회전 후)
- 현재 arm.pivotZ = 0 → 어깨와 팔 pivot이 Z 방향으로 1.96 단위 어긋남 → 분리/교차 발생

### 시도 5 (이전 세션): pivotY만 수정 → ❌ 실패
- `body.pivotY=3, arm.pivotY=5, leg.pivotY=12` 등 그룹 오프셋 설정
- pivotZ=0 고정이므로 여전히 분리

### 시도 6: setupTransforms 엔티티 회전 → ❌ 실패

**방법:**
바닐라 수영(-90° 엔티티 회전)과 동일한 원리:
```java
// PlayerEntityRendererMixin.setupTransforms
if (state.isCrawling) {
    matrices.multiply(RotationAxis.POSITIVE_X.rotation(-(QUARTER - THIRTYTWOTH)));
} else if (state.isSliding) {
    matrices.multiply(RotationAxis.POSITIVE_X.rotation(-QUARTER));
}
```
- setAngles에서 1.7.10 원본 LOCAL 값 그대로 사용 (`body.pitch=0`, `arm.pitch=Half+Eighth`)
- 수영/잠수/헤드점프: `arm.pivotZ = 2*sin(outerX)`, `leg.pivotZ = 12*sin(outerX)` 보정
- head.pitch: `m.head.pitch += bodyPitch` (엔티티 회전 상쇄)

**커밋:** `ecf0e2c`

**결과: ❌ 실패** (유저 테스트 후 보고)

---

## 세션 3 작업 내용 (시도 8, 커밋 448f235)

### 렌더 파이프라인 분석 완료

바이트코드 역공학으로 LivingEntityRenderer.render() 순서 확정:
1. entity_translate (발 위치)
2. setupTransforms (@TAIL = 여기서 우리 회전 적용)
3. scale(-1,-1,1) [Y축 뒤집힘]
4. translate(0, -1.501, 0) [모델 원점 위로 1.501 이동]
5. model.render()

→ setupTransforms에서 Rx(-79°) 적용 시 수학적으로 발=지면(Y=0), 머리=+0.38 블록.
→ heightOffset=-0.5F 는 불필요하며 오히려 0.5 블록 땅속으로 내려보냄.

### 수정된 3가지 버그

1. **heightOffset=-0.5F 제거**: 엔티티 회전 방식에서 필요 없음, 오히려 땅속으로
2. **leg.pivotZ 무조건 초기화 제거**: vanilla 스니킹 leg.pivotZ=4.0 깨지 않도록
3. **바닐라 수영 역보정 공식 수정**: leaningPitch 기반으로 정확히 상쇄 + translate(0,-1,0.3) 역보정

### 다음 세션 할 일

- 게임 내 테스트: 크롤링이 땅속으로 들어가는지 확인
- 수영/잠수 애니메이션 visual 확인
- 스니킹 시 머리/몸통 분리 현상 재테스트
- 무빙 커맨드들 작동 확인 (클라이밍, 슬라이딩, 수영, 헤드점프 등)

---

## 핵심 파일 위치

| 파일 | 역할 |
|------|------|
| `src/client/java/.../mixin/client/PlayerEntityModelMixin.java` | 모든 애니메이션 로직 |
| `src/client/java/.../client/render/AnimationUtil.java` | 각도 상수 + factor() 함수 |
| `src/main/java/.../physics/ClimbingHandler.java` | 클라이밍 상태 + 탈진 |
| `src/main/java/.../physics/CeilingClimbingHandler.java` | 천장 클라이밍 상태 + 탈진 |
| `docs/research_animation.md` | 원본 애니메이션 분석 (섹션 D: 원본 코드, 섹션 H: 버그 분석) |
| `docs/checklist_implementation.md` | 전체 구현 체크리스트 |

# 플레이테스트 발견 이슈 — 1:1 번역 재작업 (진입점)

> 실기 테스트에서 원본(1.7.10)과 다른 동작을 재발견. 각 항목을 원자 단위로 분해하여
> 1:1 번역을 다시 돌리는 방식으로 수정한다. **이 문서 자체가 작업 진입점.**

---

## 🎯 현재 포커스

```
항목 #5  — 옵션토글 4단계 순환 (disabled / easy / medium / hard)
```

작업 문서: [`focus_05_config_toggle.md`](focus_05_config_toggle.md)

---

## 📋 작업 방식 — 2-문서 워크플로우

```
세션 진입 시 읽을 문서 (고정):
  ① docs/fix/playtest_fixes.md  (이 문서)          — 포커스 확인 + 룰 확인
  ② docs/fix/focus_<NN>_<name>.md  (현재 포커스)  — 원본 근거 / 작업 기록 / 체크리스트

작업 순서:
  1. ① 읽고 "현재 포커스" 번호 확인
  2. ② (해당 포커스 파일) 열어 "진행 상황" + "완료 전 검증 체크리스트" 확인
  3. ② 의 "원본 근거" 섹션 검토 — 부족하면 Agent WebFetch 로 보완
  4. ② 의 "원자 단위 작업 목록" 중 다음 미완료 항목 진행
  5. 수정 → 컴파일 → ② 에 기록 + 체크리스트 갱신 → 커밋
  6. 포커스 모든 원자 작업 [x] + 컴파일 성공 + 검증 체크리스트 통과 시:
     ① "현재 포커스" 를 다음 번호로 갱신 + 커밋
```

---

## 포커스 순서

| 순서 | 번호 | 제목 | 작업 문서 | 상태 |
|------|------|------|----------|------|
| 1 | **#5** | 옵션토글 4단계 순환 | [focus_05_config_toggle.md](focus_05_config_toggle.md) | 🟡 진행 중 |
| 2 | **#6** | increase/decrease 작동 안 됨 | [focus_06_speed_change.md](focus_06_speed_change.md) | ⚪ 대기 |
| 3 | **#2** | 스마트무빙 상태 이상 | [focus_02_state_issues.md](focus_02_state_issues.md) | ⚪ 대기 (재현 케이스 필요) |
| 4 | **#3** | 상태 전환 조건 이상 | [focus_03_transition_conditions.md](focus_03_transition_conditions.md) | ⚪ 대기 (재현 케이스 필요) |
| 5 | **#4** | 상태 전환 키 커맨드 조합 이상 | [focus_04_key_combos.md](focus_04_key_combos.md) | ⚪ 대기 (재현 케이스 필요) |
| 6 | **#1** | 애니메이션 망가짐/이상 | [focus_01_animation.md](focus_01_animation.md) | ⚪ 대기 (재현 케이스 필요) |

---

## 1:1 번역 룰 (PORTING_RULES.md + checklist_original_audit.md 통합)

```
원칙 1. 원자 단위 원칙
   항목 하나 = 원본 동작 하나. "X 기능 구현" 같은 복수 동작 금지.

원칙 2. 완전 추출 원칙
   리서치 단계에서 원본 클래스의 모든 메서드 × 모든 분기 × 모든 상수를
   빠짐없이 추출. 기록되지 않은 동작은 구현 대상이 아니다.

원칙 3. 추적 가능성 원칙
   코드에 TODO / [미확인] / 빈 메서드가 존재하면 반드시 대응 항목이
   해당 포커스 파일 또는 PENDING_RESEARCH.md 에 등록되어 있어야 한다.

원칙 4. 미확인 차단 원칙 (감사 규칙 B)
   설계 근거(리서치 파일의 원본 코드)가 없으면 구현하지 않는다.
   "아마 이럴 것이다"로 코드를 작성하지 않는다.
   리서치 파일에 없는 동작은 → 리서치 파일을 먼저 보완한다.

원칙 5. 완료는 검증 이후 원칙
   항목을 [x]로 바꾸기 전에 완료 전 검증 체크리스트를 통과해야 한다.

감사 규칙 A. 전체 읽기 원칙
   리서치 파일은 처음부터 끝까지 전부 읽는다.
   대응 구현 파일도 처음부터 끝까지 전부 읽는다.

감사 규칙 C. 즉시 수정 원칙
   불일치를 발견한 순간 그 자리에서 수정한다.
   수정 후 컴파일 확인.

감사 규칙 D. 불일치 3분류
   [오역]   구현이 원본과 다른 값/조건/로직을 사용한다
   [누락]   원본의 분기·상수·조건이 구현에 존재하지 않는다
   [잉여]   원본에 없는 로직이 구현에 추가되어 있다

원자 단위 예시
   허용: "isClimbing=true일 때 매 틱 fallDistance=0 으로 리셋"
   금지: "클라이밍 구현" (복수 동작 포함)
   금지: "TODO Phase N 처리" (무엇인지 불명확)

완료 전 검증 체크리스트 (각 원자 작업마다)
   □ 의존 리서치 문서에서 관련 메서드/분기를 다시 읽었다
   □ 리서치 문서의 동작 설명과 구현 코드가 1:1로 대응된다
   □ 리서치 문서에 기록된 모든 분기(if/else)가 코드에 존재한다
   □ 리서치 문서에 기록된 모든 상수가 코드에 반영되었다
   □ 구현 중 발견한 신규 누락은 신규 항목으로 등록했다
   □ 메서드 호출 타이밍(훅 위치)이 원본과 일치한다
   □ 컴파일 성공
```

**우선순위**: 이 문서 > PORTING_RULES.md > CLAUDE.md > 개인 판단.

---

## 변경 이력

- **2026-04-23**: 문서 생성. 플레이테스트 발견 이슈 6종 기록. 포커스 순서 #5 → #6 → #2 → #3 → #4 → #1 확정.
- **2026-04-23**: `docs/fix/` 폴더로 이동. 각 포커스별 작업 기록 파일(`focus_NN_*.md`) 6개 분리. 2-문서 워크플로우 명시.
